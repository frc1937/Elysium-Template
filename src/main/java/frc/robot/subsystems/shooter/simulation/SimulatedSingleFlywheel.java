package frc.robot.subsystems.shooter.simulation;

import frc.lib.generic.motor.Motor;
import frc.robot.subsystems.shooter.SingleFlywheelIO;

public class SimulatedSingleFlywheel extends SingleFlywheelIO {
    public SimulatedSingleFlywheel(String name, Motor motor) {
        super(name);
    }
}
